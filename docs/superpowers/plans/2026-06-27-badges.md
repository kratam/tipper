# Badge / achievement rendszer — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Jelvény-rendszer, amely a hivatalos Ranglista meccs-, forduló- és torna-eredményeiből determinisztikusan badge-eket oszt, profil-oldalon és a ranglistán megjeleníti, és in-app értesítést küld.

**Architecture:** Pure derive-and-diff evaluator (a `scoring.ts` mintájára): minden triggernél (meccs lepontozva / forduló lezárva / torna lezárva) újraszámoljuk az érintett játékos badge-állapotát a forrásadatból, diff-eljük a tárolttal, és csak a növekményt írjuk + jelezzük. Az abszolút badge-ek tisztán a `bets`-ből derive-álódnak; a relatív/esemény-badge-ek egy `user_badge_events` idempotencia-táblát használnak.

**Tech Stack:** Next.js 16 App Router, Drizzle ORM (Neon HTTP driver), TypeScript strict, Vitest (pure logika TDD), next-intl, Lucide ikonok, Tailwind v4, Biome.

## Global Constraints

- Magyar UI szövegek **mindig ékezetekkel**; minden UI szöveg `useTranslations()` / `getTranslations()` — nincs hardcoded string.
- Séma-változás **kizárólag** `npm run db:generate` → `npm run db:migrate` úton (lokálisan, `dev` Neon branch). Soha `drizzle-kit push` / kézi SQL / Neon MCP migráció.
- DB műveletek Drizzle query API-val (nem raw SQL), kivéve a meglévő `sql`\`...\` aggregációs mintát.
- Server Actions: `"use server"` + `getCurrentUser()` ellenőrzés.
- Commit: conventional commits (feat/fix/chore/docs/test). Gyakori commitok.
- Nincs DB-tranzakció (Neon HTTP driver); idempotenciát unique index + „csak előre" frissítés ad.
- Hatókör: **minden badge-evaluáció a hivatalos Ranglista csoportra** (`isOfficial = true`, `OFFICIAL_GROUP_SLUG = "hivatalos"`).
- A badge **nem** ír `token_ledger`-t, nem érinti a `token_type` enumot.
- Design tokenek: arany = `text-gold`/`bg-gold`/`primary`; az `accent` a halvány hover-háttér, NEM arany. Egységes `ui/button.tsx`, `ui/badge.tsx`, `ui/tabs.tsx` — ne hozz létre ad-hocot.

---

## Badge-katalógus (referencia minden taskhoz)

| key | mechanic | scope | trigger | bronz | ezüst | arany | Lucide |
|---|---|---|---|---|---|---|---|
| `win_streak` | streak | absolute | match | 3 | 5 | 8 | `dice-5` |
| `exact_streak` | streak | absolute | match | 2 | 3 | 4 | `target` |
| `jackpot` | odds | absolute | match | 3.0 | 5.0 | 7.0 | `gem` |
| `perfect_day` | count | absolute | round | 1 | 3 | 7 | `sun` |
| `oracle` | count | relative(event) | tournament | 1 | 3 | 6 | `sparkles` |
| `round_winner` | count | relative(event) | round | 1 | 3 | 7 | `medal` |
| `champion` | count | relative(event) | tournament | 1 | 3 | 5 | `crown` |
| `podium` | count | relative(event) | tournament | 1 | 3 | 7 | `award` |
| `throne` | count | relative(event) | round | 1 | 3 | 10 | `star` |
| `red_lantern` | count | relative(event) | tournament | 1 | 3 | 5 | `flame` |

- **streak:** a tier-t a sorozat-csúcs adja; `value = count = streak-hossz`, `bestValue = streak-hossz`.
- **odds (jackpot):** a tier-t a legnagyobb behozott odds adja; `value = maxOdds`, `count = nagy-odds nyertes tippek száma`, `bestValue = maxOdds`. Belépő: `oddsAtBet ≥ 3.0` ÉS `stake ≥ Ranglista tokenPerMatch (100)`.
- **count:** a tier-t a darabszám adja; `value = count`, `bestValue = null`.
- **event-badge** (`oracle`, `round_winner`, `champion`, `podium`, `throne`, `red_lantern`): a `count` a `user_badge_events` sorok száma; idempotens `eventKey`-jel.
  - `eventKey`: `champion`/`podium`/`red_lantern`/`oracle`/`throne` → `tournamentId`; `round_winner` → `${tournamentId}:${round}`.

---

## File Structure

**Új fájlok:**
- `src/db/schema.ts` (MÓDOSÍT) — `userBadges`, `userBadgeEvents` táblák + relations.
- `src/lib/badges/catalog.ts` — `BADGES` katalógus, típusok, `tierFor`, `getBadge`.
- `src/lib/badges/evaluate.ts` — pure derive-függvények (streak/jackpot/perfect-day) + `diffBadge`.
- `src/lib/badges/relative.ts` — pure relatív helyezés-logika (round winners, placements).
- `src/queries/badges.ts` — `loadUserBadges`, badge upsert, event-insert, ranglista batch.
- `src/lib/badges/award.ts` — orchestráció: derive→diff→persist→notify (meccs/forduló/torna).
- `src/queries/profile.ts` — profil-oldal adatai (statok + badges + láthatóság).
- `src/components/badge-icon.tsx` — Lucide ikon + tier-szín.
- `src/components/trophy-cabinet.tsx` — trófea-szekrény rács.
- `src/components/leaderboard-badges.tsx` — ranglista ikon-sor (top N).
- `src/app/[locale]/u/[userId]/page.tsx` — profil-oldal.
- `tests/lib/badges-evaluate.test.ts`, `tests/lib/badges-relative.test.ts`, `tests/lib/badges-catalog.test.ts`, `tests/lib/badges-diff.test.ts`, `tests/lib/profile-stats.test.ts`.

**Módosított fájlok:**
- `src/lib/sync.ts` — `scoreMatch` után meccs-trigger; `syncFixtures` után forduló-trigger.
- `src/actions/admin.ts` — `finishTournament` után torna-trigger.
- `src/lib/notification-display.ts` — badge-ág `data`-átadás + aggregált ág.
- `src/lib/notifications.ts` — `createBadgeNotifications` aggregált helper.
- `src/components/leaderboard/*` (a `getGroupLeaderboard` fogyasztói) — badge ikon-sor.
- `messages/hu.json`, `messages/en.json` — `badges.*`, `notifications.badge.*`, `profile.*` bővítés.
- `src/app/globals.css` — `--color-bronze`, `--color-silver` tokenek.
- `src/components/nav.tsx` (vagy a meglévő user-menü) — link a saját profilra.

---

## Task 1: `user_badges` + `user_badge_events` séma + migráció

**Files:**
- Modify: `src/db/schema.ts` (a `notificationRecipients` blokk után, ~335; relations a végére)
- Migration: generált `drizzle/NNNN_*.sql`

**Interfaces:**
- Produces: `userBadges` tábla (`userId`, `badgeKey`, `tier smallint`, `count int`, `bestValue numeric null`, `firstEarnedAt`, `updatedAt`), unique `(userId, badgeKey)`. `userBadgeEvents` tábla (`userId`, `badgeKey`, `eventKey`, `createdAt`), unique `(userId, badgeKey, eventKey)`.

- [ ] **Step 1: Séma definíció hozzáadása** `src/db/schema.ts`-be (a `notificationRecipients` után):

```ts
export const userBadges = pgTable(
  "user_badges",
  {
    id: uuid("id").defaultRandom().primaryKey(),
    userId: uuid("user_id")
      .references(() => users.id, { onDelete: "cascade" })
      .notNull(),
    badgeKey: text("badge_key").notNull(),
    tier: integer("tier").notNull(), // 1=bronz, 2=ezüst, 3=arany
    count: integer("count").default(0).notNull(),
    bestValue: decimal("best_value", { precision: 8, scale: 2 }),
    firstEarnedAt: timestamp("first_earned_at", { withTimezone: true }).defaultNow().notNull(),
    updatedAt: timestamp("updated_at", { withTimezone: true }).defaultNow().notNull(),
  },
  (table) => [
    uniqueIndex("user_badge_idx").on(table.userId, table.badgeKey),
    index("user_badges_user_idx").on(table.userId),
  ],
);

export const userBadgeEvents = pgTable(
  "user_badge_events",
  {
    id: uuid("id").defaultRandom().primaryKey(),
    userId: uuid("user_id")
      .references(() => users.id, { onDelete: "cascade" })
      .notNull(),
    badgeKey: text("badge_key").notNull(),
    eventKey: text("event_key").notNull(),
    createdAt: timestamp("created_at", { withTimezone: true }).defaultNow().notNull(),
  },
  (table) => [
    uniqueIndex("user_badge_event_idx").on(table.userId, table.badgeKey, table.eventKey),
  ],
);

export const userBadgesRelations = relations(userBadges, ({ one }) => ({
  user: one(users, { fields: [userBadges.userId], references: [users.id] }),
}));
```

- [ ] **Step 2: Migráció generálása**

Run: `npm run db:generate`
Expected: új `drizzle/NNNN_*.sql` a két `CREATE TABLE`-lel + indexekkel; nincs egyéb drift.

- [ ] **Step 3: Migráció futtatása a dev branchen**

Run: `npm run db:migrate`
Expected: sikeres (exit 0), a két tábla létrejön.

- [ ] **Step 4: Build ellenőrzés**

Run: `npm run check`
Expected: nincs lint/type hiba az új sémára.

- [ ] **Step 5: Commit**

```bash
git add src/db/schema.ts drizzle/
git commit -m "feat(badges): user_badges és user_badge_events séma + migráció"
```

---

## Task 2: Badge-katalógus + `tierFor` (pure, TDD)

**Files:**
- Create: `src/lib/badges/catalog.ts`
- Test: `tests/lib/badges-catalog.test.ts`

**Interfaces:**
- Consumes: —
- Produces:
  - `type BadgeMechanic = "streak" | "odds" | "count"`
  - `type BadgeScope = "absolute" | "relative"`
  - `type BadgeTrigger = "match" | "round" | "tournament"`
  - `interface BadgeDef { key: string; mechanic: BadgeMechanic; scope: BadgeScope; trigger: BadgeTrigger; thresholds: { bronze: number; silver: number; gold: number }; icon: string; minStake?: number }`
  - `const BADGES: readonly BadgeDef[]`
  - `function getBadge(key: string): BadgeDef | undefined`
  - `function tierFor(def: BadgeDef, value: number): 0 | 1 | 2 | 3` (0 = nincs)

- [ ] **Step 1: Failing test** `tests/lib/badges-catalog.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { BADGES, getBadge, tierFor } from "@/lib/badges/catalog";

describe("badge catalog", () => {
  it("has 10 unique badge keys", () => {
    expect(BADGES).toHaveLength(10);
    expect(new Set(BADGES.map((b) => b.key)).size).toBe(10);
  });

  it("tierFor returns 0 below bronze", () => {
    const winStreak = getBadge("win_streak")!;
    expect(tierFor(winStreak, 2)).toBe(0);
    expect(tierFor(winStreak, 3)).toBe(1);
    expect(tierFor(winStreak, 5)).toBe(2);
    expect(tierFor(winStreak, 8)).toBe(3);
    expect(tierFor(winStreak, 12)).toBe(3);
  });

  it("jackpot tiers on odds value", () => {
    const jackpot = getBadge("jackpot")!;
    expect(jackpot.mechanic).toBe("odds");
    expect(jackpot.minStake).toBe(100);
    expect(tierFor(jackpot, 2.9)).toBe(0);
    expect(tierFor(jackpot, 3.0)).toBe(1);
    expect(tierFor(jackpot, 6.5)).toBe(2);
    expect(tierFor(jackpot, 9.0)).toBe(3);
  });
});
```

- [ ] **Step 2: Run → FAIL** — `npm run test -- badges-catalog` → "Cannot find module".

- [ ] **Step 3: Implementáció** `src/lib/badges/catalog.ts`:

```ts
export type BadgeMechanic = "streak" | "odds" | "count";
export type BadgeScope = "absolute" | "relative";
export type BadgeTrigger = "match" | "round" | "tournament";

export interface BadgeDef {
  key: string;
  mechanic: BadgeMechanic;
  scope: BadgeScope;
  trigger: BadgeTrigger;
  thresholds: { bronze: number; silver: number; gold: number };
  icon: string;
  minStake?: number;
}

export const BADGES: readonly BadgeDef[] = [
  { key: "win_streak", mechanic: "streak", scope: "absolute", trigger: "match", thresholds: { bronze: 3, silver: 5, gold: 8 }, icon: "dice-5" },
  { key: "exact_streak", mechanic: "streak", scope: "absolute", trigger: "match", thresholds: { bronze: 2, silver: 3, gold: 4 }, icon: "target" },
  { key: "jackpot", mechanic: "odds", scope: "absolute", trigger: "match", thresholds: { bronze: 3.0, silver: 5.0, gold: 7.0 }, icon: "gem", minStake: 100 },
  { key: "perfect_day", mechanic: "count", scope: "absolute", trigger: "round", thresholds: { bronze: 1, silver: 3, gold: 7 }, icon: "sun" },
  { key: "oracle", mechanic: "count", scope: "relative", trigger: "tournament", thresholds: { bronze: 1, silver: 3, gold: 6 }, icon: "sparkles" },
  { key: "round_winner", mechanic: "count", scope: "relative", trigger: "round", thresholds: { bronze: 1, silver: 3, gold: 7 }, icon: "medal" },
  { key: "champion", mechanic: "count", scope: "relative", trigger: "tournament", thresholds: { bronze: 1, silver: 3, gold: 5 }, icon: "crown" },
  { key: "podium", mechanic: "count", scope: "relative", trigger: "tournament", thresholds: { bronze: 1, silver: 3, gold: 7 }, icon: "award" },
  { key: "throne", mechanic: "count", scope: "relative", trigger: "round", thresholds: { bronze: 1, silver: 3, gold: 10 }, icon: "star" },
  { key: "red_lantern", mechanic: "count", scope: "relative", trigger: "tournament", thresholds: { bronze: 1, silver: 3, gold: 5 }, icon: "flame" },
];

const BY_KEY = new Map(BADGES.map((b) => [b.key, b]));
export function getBadge(key: string): BadgeDef | undefined {
  return BY_KEY.get(key);
}

export function tierFor(def: BadgeDef, value: number): 0 | 1 | 2 | 3 {
  const { bronze, silver, gold } = def.thresholds;
  if (value >= gold) return 3;
  if (value >= silver) return 2;
  if (value >= bronze) return 1;
  return 0;
}
```

- [ ] **Step 4: Run → PASS** — `npm run test -- badges-catalog`.

- [ ] **Step 5: Commit**

```bash
git add src/lib/badges/catalog.ts tests/lib/badges-catalog.test.ts
git commit -m "feat(badges): katalógus + tierFor (pure, tesztelt)"
```

---

## Task 3: Abszolút meccs-badge derive (streak + jackpot, pure, TDD)

**Files:**
- Create: `src/lib/badges/evaluate.ts`
- Test: `tests/lib/badges-evaluate.test.ts`

**Interfaces:**
- Consumes: `BadgeDef`, `tierFor`, `getBadge` (Task 2).
- Produces:
  - `interface ScoredBet { matchId: string; scheduledAt: Date; result1x2Correct: boolean; exactScoreCorrect: boolean; oddsAtBet: number | null; stake: number }`
  - `interface BadgeProgress { badgeKey: string; tier: 0 | 1 | 2 | 3; value: number; count: number; bestValue: number | null }`
  - `function computeWinStreak(orderedBets: ScoredBet[]): number`
  - `function computeExactStreak(orderedBets: ScoredBet[]): number`
  - `function computeJackpot(bets: ScoredBet[], minStake: number): { count: number; maxOdds: number }`
  - `function computeAbsoluteMatchBadges(orderedBets: ScoredBet[]): BadgeProgress[]` — `win_streak`, `exact_streak`, `jackpot` progressje (csak ahol `tier > 0` VAGY `count > 0`).

A bemenet **időrendben** (scheduledAt, majd matchId tie-break) rendezett, **csak lepontozott** (a hívó szűr `payout != null`-ra). „Nyerő" = `result1x2Correct === true` (NEM a `payout > 0`, mert a részleges visszatérítés is pozitív payout).

- [ ] **Step 1: Failing test** `tests/lib/badges-evaluate.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import {
  computeAbsoluteMatchBadges,
  computeExactStreak,
  computeJackpot,
  computeWinStreak,
  type ScoredBet,
} from "@/lib/badges/evaluate";

function bet(p: Partial<ScoredBet>): ScoredBet {
  return {
    matchId: "m",
    scheduledAt: new Date("2026-01-01"),
    result1x2Correct: false,
    exactScoreCorrect: false,
    oddsAtBet: null,
    stake: 100,
    ...p,
  };
}

describe("computeWinStreak", () => {
  it("returns longest uninterrupted 1X2-correct run", () => {
    const bets = [true, true, false, true, true, true, false].map((w) =>
      bet({ result1x2Correct: w }),
    );
    expect(computeWinStreak(bets)).toBe(3);
  });
  it("returns 0 for no wins", () => {
    expect(computeWinStreak([bet({}), bet({})])).toBe(0);
  });
});

describe("computeExactStreak", () => {
  it("counts only exact-score runs", () => {
    const bets = [true, true, false].map((e) => bet({ exactScoreCorrect: e }));
    expect(computeExactStreak(bets)).toBe(2);
  });
});

describe("computeJackpot", () => {
  it("counts winning bets at odds>=3 with stake>=minStake, tracks max odds", () => {
    const bets = [
      bet({ result1x2Correct: true, oddsAtBet: 4.0, stake: 100 }),
      bet({ result1x2Correct: true, oddsAtBet: 9.0, stake: 100 }),
      bet({ result1x2Correct: true, oddsAtBet: 5.0, stake: 50 }), // stake too low
      bet({ result1x2Correct: false, oddsAtBet: 10.0, stake: 100 }), // lost
      bet({ result1x2Correct: true, oddsAtBet: 2.5, stake: 100 }), // odds too low
    ];
    expect(computeJackpot(bets, 100)).toEqual({ count: 2, maxOdds: 9.0 });
  });
  it("returns zero when none qualify", () => {
    expect(computeJackpot([bet({ result1x2Correct: true, oddsAtBet: 2, stake: 100 })], 100))
      .toEqual({ count: 0, maxOdds: 0 });
  });
});

describe("computeAbsoluteMatchBadges", () => {
  it("emits win_streak tier and jackpot with bestValue", () => {
    const bets = [
      bet({ result1x2Correct: true, oddsAtBet: 6.0, stake: 100 }),
      bet({ result1x2Correct: true }),
      bet({ result1x2Correct: true }),
    ];
    const out = computeAbsoluteMatchBadges(bets);
    const ws = out.find((b) => b.badgeKey === "win_streak")!;
    expect(ws.tier).toBe(1); // 3-run = bronze
    expect(ws.count).toBe(3);
    const jp = out.find((b) => b.badgeKey === "jackpot")!;
    expect(jp.tier).toBe(2); // 6.0 odds = silver
    expect(jp.count).toBe(1);
    expect(jp.bestValue).toBe(6.0);
  });
});
```

- [ ] **Step 2: Run → FAIL.**

- [ ] **Step 3: Implementáció** `src/lib/badges/evaluate.ts`:

```ts
import { getBadge, tierFor } from "@/lib/badges/catalog";

export interface ScoredBet {
  matchId: string;
  scheduledAt: Date;
  result1x2Correct: boolean;
  exactScoreCorrect: boolean;
  oddsAtBet: number | null;
  stake: number;
}

export interface BadgeProgress {
  badgeKey: string;
  tier: 0 | 1 | 2 | 3;
  value: number;
  count: number;
  bestValue: number | null;
}

function longestRun(flags: boolean[]): number {
  let best = 0;
  let cur = 0;
  for (const f of flags) {
    cur = f ? cur + 1 : 0;
    if (cur > best) best = cur;
  }
  return best;
}

export function computeWinStreak(orderedBets: ScoredBet[]): number {
  return longestRun(orderedBets.map((b) => b.result1x2Correct));
}

export function computeExactStreak(orderedBets: ScoredBet[]): number {
  return longestRun(orderedBets.map((b) => b.exactScoreCorrect));
}

export function computeJackpot(bets: ScoredBet[], minStake: number): { count: number; maxOdds: number } {
  let count = 0;
  let maxOdds = 0;
  for (const b of bets) {
    if (b.result1x2Correct && b.oddsAtBet !== null && b.oddsAtBet >= 3.0 && b.stake >= minStake) {
      count++;
      if (b.oddsAtBet > maxOdds) maxOdds = b.oddsAtBet;
    }
  }
  return { count, maxOdds };
}

export function computeAbsoluteMatchBadges(orderedBets: ScoredBet[]): BadgeProgress[] {
  const out: BadgeProgress[] = [];

  const winStreak = computeWinStreak(orderedBets);
  const wsDef = getBadge("win_streak")!;
  out.push({ badgeKey: "win_streak", tier: tierFor(wsDef, winStreak), value: winStreak, count: winStreak, bestValue: winStreak });

  const exactStreak = computeExactStreak(orderedBets);
  const esDef = getBadge("exact_streak")!;
  out.push({ badgeKey: "exact_streak", tier: tierFor(esDef, exactStreak), value: exactStreak, count: exactStreak, bestValue: exactStreak });

  const jpDef = getBadge("jackpot")!;
  const jp = computeJackpot(orderedBets, jpDef.minStake ?? 0);
  out.push({ badgeKey: "jackpot", tier: tierFor(jpDef, jp.maxOdds), value: jp.maxOdds, count: jp.count, bestValue: jp.maxOdds || null });

  return out;
}
```

- [ ] **Step 4: Run → PASS.**

- [ ] **Step 5: Commit**

```bash
git add src/lib/badges/evaluate.ts tests/lib/badges-evaluate.test.ts
git commit -m "feat(badges): abszolút meccs-badge derive (streak/jackpot, tesztelt)"
```

---

## Task 4: Nyerő nap (perfect_day) derive + diff helper (pure, TDD)

**Files:**
- Modify: `src/lib/badges/evaluate.ts`
- Test: `tests/lib/badges-evaluate.test.ts` (bővítés), `tests/lib/badges-diff.test.ts` (új)

**Interfaces:**
- Consumes: `ScoredBet`, `BadgeProgress` (Task 3).
- Produces:
  - `function computePerfectDays(bets: ScoredBet[], minBets?: number): number` — `date(scheduledAt)` szerint csoportosít; egy nap „nyerő", ha ≥`minBets` (default 4) tipp ÉS mind `result1x2Correct`. Visszaadja az ilyen napok számát. A dátum-kulcs UTC (`toISOString().slice(0,10)`).
  - `interface StoredBadge { tier: number; count: number; bestValue: number | null }`
  - `function diffBadge(stored: StoredBadge | undefined, derived: BadgeProgress): "new" | "upgraded" | null` — `new` ha nincs stored és derived.tier ≥ 1; `upgraded` ha derived.tier > stored.tier; egyébként `null` (a count/bestValue frissülhet, de értesítés csak tier-lépésre).

- [ ] **Step 1: Failing tesztek.** `tests/lib/badges-evaluate.test.ts`-be:

```ts
import { computePerfectDays } from "@/lib/badges/evaluate";

describe("computePerfectDays", () => {
  it("counts days with >=4 bets all won", () => {
    const d1 = new Date("2026-01-01T18:00:00Z");
    const d2 = new Date("2026-01-02T18:00:00Z");
    const bets = [
      ...Array(4).fill(0).map(() => bet({ scheduledAt: d1, result1x2Correct: true })),
      ...Array(4).fill(0).map((_, i) => bet({ scheduledAt: d2, result1x2Correct: i !== 0 })), // one loss
      ...Array(3).fill(0).map(() => bet({ scheduledAt: d1, result1x2Correct: true })), // only 3 -> handled by grouping
    ];
    // d1 has 7 bets all won -> perfect; d2 has a loss -> not perfect
    expect(computePerfectDays(bets)).toBe(1);
  });
});
```

`tests/lib/badges-diff.test.ts` (új):

```ts
import { describe, expect, it } from "vitest";
import { diffBadge } from "@/lib/badges/evaluate";

describe("diffBadge", () => {
  it("new when no stored and tier>=1", () => {
    expect(diffBadge(undefined, { badgeKey: "x", tier: 1, value: 3, count: 3, bestValue: 3 })).toBe("new");
  });
  it("null when no stored and tier 0", () => {
    expect(diffBadge(undefined, { badgeKey: "x", tier: 0, value: 2, count: 2, bestValue: 2 })).toBe(null);
  });
  it("upgraded when tier increases", () => {
    expect(diffBadge({ tier: 1, count: 3, bestValue: 3 }, { badgeKey: "x", tier: 2, value: 5, count: 5, bestValue: 5 })).toBe("upgraded");
  });
  it("null when tier unchanged", () => {
    expect(diffBadge({ tier: 2, count: 5, bestValue: 5 }, { badgeKey: "x", tier: 2, value: 6, count: 6, bestValue: 6 })).toBe(null);
  });
});
```

- [ ] **Step 2: Run → FAIL.**

- [ ] **Step 3: Implementáció** — `evaluate.ts`-be:

```ts
export function computePerfectDays(bets: ScoredBet[], minBets = 4): number {
  const byDay = new Map<string, { total: number; won: number }>();
  for (const b of bets) {
    const day = b.scheduledAt.toISOString().slice(0, 10);
    const e = byDay.get(day) ?? { total: 0, won: 0 };
    e.total++;
    if (b.result1x2Correct) e.won++;
    byDay.set(day, e);
  }
  let count = 0;
  for (const { total, won } of byDay.values()) {
    if (total >= minBets && won === total) count++;
  }
  return count;
}

export interface StoredBadge {
  tier: number;
  count: number;
  bestValue: number | null;
}

export function diffBadge(stored: StoredBadge | undefined, derived: BadgeProgress): "new" | "upgraded" | null {
  if (!stored) return derived.tier >= 1 ? "new" : null;
  if (derived.tier > stored.tier) return "upgraded";
  return null;
}
```

- [ ] **Step 4: Run → PASS.**

- [ ] **Step 5: Commit**

```bash
git add src/lib/badges/evaluate.ts tests/lib/badges-evaluate.test.ts tests/lib/badges-diff.test.ts
git commit -m "feat(badges): perfect_day derive + diffBadge (tesztelt)"
```

---

## Task 5: Relatív helyezés-logika (pure, TDD)

**Files:**
- Create: `src/lib/badges/relative.ts`
- Test: `tests/lib/badges-relative.test.ts`

**Interfaces:**
- Consumes: —
- Produces:
  - `interface RankRow { userId: string; profit: number; rank: number }` (a `getGroupLeaderboard` kimenetének részhalmaza)
  - `function roundWinners(netByUser: { userId: string; net: number }[]): string[]` — a legnagyobb nettójú userId-k (holtverseny → több). Üres / csupa ≤0 esetén: a max akkor is győztes, ha pozitív; ha minden net ≤ 0, üres tömb (nincs „győztes" veszteséges fordulóban).
  - `function tournamentPlacements(ranking: RankRow[]): { champions: string[]; podium: string[]; lastPlace: string[] }` — `champions` = rank 1; `podium` = rank ≤ 3; `lastPlace` = a legnagyobb rank (holtverseny → több). Legalább 3 fős mezőny kell (`ranking.length >= 3`), különben minden lista üres (kis-mezőny védelem).
  - `function currentLeader(ranking: RankRow[]): string | null` — rank 1 userId, ha `ranking.length >= 3`, különben `null` (a `throne`-hoz).

- [ ] **Step 1: Failing test** `tests/lib/badges-relative.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { currentLeader, roundWinners, tournamentPlacements } from "@/lib/badges/relative";

describe("roundWinners", () => {
  it("returns the single top earner", () => {
    expect(roundWinners([{ userId: "a", net: 50 }, { userId: "b", net: 120 }, { userId: "c", net: 30 }])).toEqual(["b"]);
  });
  it("returns all tied top earners", () => {
    expect(roundWinners([{ userId: "a", net: 120 }, { userId: "b", net: 120 }])).toEqual(["a", "b"]);
  });
  it("returns empty when all non-positive", () => {
    expect(roundWinners([{ userId: "a", net: -10 }, { userId: "b", net: 0 }])).toEqual([]);
  });
});

describe("tournamentPlacements", () => {
  const ranking = [
    { userId: "a", profit: 300, rank: 1 },
    { userId: "b", profit: 200, rank: 2 },
    { userId: "c", profit: 100, rank: 3 },
    { userId: "d", profit: 10, rank: 4 },
  ];
  it("splits champion / podium / last", () => {
    const r = tournamentPlacements(ranking);
    expect(r.champions).toEqual(["a"]);
    expect(r.podium).toEqual(["a", "b", "c"]);
    expect(r.lastPlace).toEqual(["d"]);
  });
  it("is empty for fewer than 3 players", () => {
    const r = tournamentPlacements(ranking.slice(0, 2));
    expect(r).toEqual({ champions: [], podium: [], lastPlace: [] });
  });
});

describe("currentLeader", () => {
  it("returns rank-1 user when >=3 players", () => {
    expect(currentLeader([
      { userId: "a", profit: 1, rank: 1 },
      { userId: "b", profit: 0, rank: 2 },
      { userId: "c", profit: -1, rank: 3 },
    ])).toBe("a");
  });
  it("returns null for small field", () => {
    expect(currentLeader([{ userId: "a", profit: 1, rank: 1 }])).toBe(null);
  });
});
```

- [ ] **Step 2: Run → FAIL.**

- [ ] **Step 3: Implementáció** `src/lib/badges/relative.ts`:

```ts
export interface RankRow {
  userId: string;
  profit: number;
  rank: number;
}

const MIN_FIELD = 3;

export function roundWinners(netByUser: { userId: string; net: number }[]): string[] {
  if (netByUser.length === 0) return [];
  const max = Math.max(...netByUser.map((r) => r.net));
  if (max <= 0) return [];
  return netByUser.filter((r) => r.net === max).map((r) => r.userId);
}

export function tournamentPlacements(ranking: RankRow[]): {
  champions: string[];
  podium: string[];
  lastPlace: string[];
} {
  if (ranking.length < MIN_FIELD) return { champions: [], podium: [], lastPlace: [] };
  const maxRank = Math.max(...ranking.map((r) => r.rank));
  return {
    champions: ranking.filter((r) => r.rank === 1).map((r) => r.userId),
    podium: ranking.filter((r) => r.rank <= 3).map((r) => r.userId),
    lastPlace: ranking.filter((r) => r.rank === maxRank).map((r) => r.userId),
  };
}

export function currentLeader(ranking: RankRow[]): string | null {
  if (ranking.length < MIN_FIELD) return null;
  return ranking.find((r) => r.rank === 1)?.userId ?? null;
}
```

- [ ] **Step 4: Run → PASS.**

- [ ] **Step 5: Commit**

```bash
git add src/lib/badges/relative.ts tests/lib/badges-relative.test.ts
git commit -m "feat(badges): relatív helyezés-logika (pure, tesztelt)"
```

---

## Task 6: Badge persist/query réteg (`src/queries/badges.ts`)

**Files:**
- Create: `src/queries/badges.ts`

**Interfaces:**
- Consumes: `userBadges`, `userBadgeEvents` (Task 1); `BadgeProgress`, `StoredBadge` (Task 3-4); `getBadge`, `tierFor` (Task 2).
- Produces:
  - `async function loadUserBadges(userId: string): Promise<Map<string, StoredBadge>>`
  - `async function upsertUserBadge(userId: string, p: BadgeProgress): Promise<void>` — `onConflictDoUpdate` `(userId, badgeKey)`-re; tier/count/bestValue **monoton előre** (`GREATEST`-szerű: csak ha nő); `updatedAt = now`.
  - `async function recordBadgeEvent(userId: string, badgeKey: string, eventKey: string): Promise<boolean>` — insert `onConflictDoNothing`; `true` ha új sor keletkezett (a `.returning()` hossza alapján).
  - `async function countBadgeEvents(userId: string, badgeKey: string): Promise<number>`
  - `async function loadBadgesForUsers(userIds: string[]): Promise<Map<string, StoredBadge & { badgeKey: string }[]>>` — ranglista-batch (N+1 nélkül, egy `inArray` lekérdezés), userId → badge-sorok.

- [ ] **Step 1: Implementáció** (DB-réteg, integrációs teszt opcionális — a logika a pure Task-okban fedett):

```ts
import "server-only";
import { and, eq, inArray } from "drizzle-orm";
import { db } from "@/db";
import { userBadgeEvents, userBadges } from "@/db/schema";
import type { BadgeProgress, StoredBadge } from "@/lib/badges/evaluate";

export async function loadUserBadges(userId: string): Promise<Map<string, StoredBadge>> {
  const rows = await db.select().from(userBadges).where(eq(userBadges.userId, userId));
  return new Map(
    rows.map((r) => [r.badgeKey, { tier: r.tier, count: r.count, bestValue: r.bestValue ? Number(r.bestValue) : null }]),
  );
}

export async function upsertUserBadge(userId: string, p: BadgeProgress): Promise<void> {
  await db
    .insert(userBadges)
    .values({
      userId,
      badgeKey: p.badgeKey,
      tier: p.tier,
      count: p.count,
      bestValue: p.bestValue === null ? null : String(p.bestValue),
    })
    .onConflictDoUpdate({
      target: [userBadges.userId, userBadges.badgeKey],
      set: {
        tier: p.tier,
        count: p.count,
        bestValue: p.bestValue === null ? null : String(p.bestValue),
        updatedAt: new Date(),
      },
    });
}

export async function recordBadgeEvent(userId: string, badgeKey: string, eventKey: string): Promise<boolean> {
  const inserted = await db
    .insert(userBadgeEvents)
    .values({ userId, badgeKey, eventKey })
    .onConflictDoNothing()
    .returning({ id: userBadgeEvents.id });
  return inserted.length > 0;
}

export async function countBadgeEvents(userId: string, badgeKey: string): Promise<number> {
  const rows = await db
    .select({ id: userBadgeEvents.id })
    .from(userBadgeEvents)
    .where(and(eq(userBadgeEvents.userId, userId), eq(userBadgeEvents.badgeKey, badgeKey)));
  return rows.length;
}

export async function loadBadgesForUsers(
  userIds: string[],
): Promise<Map<string, Array<StoredBadge & { badgeKey: string }>>> {
  const result = new Map<string, Array<StoredBadge & { badgeKey: string }>>();
  if (userIds.length === 0) return result;
  const rows = await db.select().from(userBadges).where(inArray(userBadges.userId, userIds));
  for (const r of rows) {
    const arr = result.get(r.userId) ?? [];
    arr.push({ badgeKey: r.badgeKey, tier: r.tier, count: r.count, bestValue: r.bestValue ? Number(r.bestValue) : null });
    result.set(r.userId, arr);
  }
  return result;
}
```

**Megjegyzés a monoton frissítésről:** a derive mindig a teljes előzményből számol, így a `tier`/`count` természetesen monoton nő; explicit `GREATEST` nem kell, mert a derived érték sosem kisebb a tárolt valós állapotnál (kivéve adatjavítás — az szándékolt felülírás).

- [ ] **Step 2: Build** — `npm run check` → nincs hiba.

- [ ] **Step 3: Commit**

```bash
git add src/queries/badges.ts
git commit -m "feat(badges): persist/query réteg (upsert, event, batch)"
```

---

## Task 7: Award orchestráció (`src/lib/badges/award.ts`)

**Files:**
- Create: `src/lib/badges/award.ts`

**Interfaces:**
- Consumes: Task 2-6 minden exportja; `createBadgeNotifications` (Task 8 — ld. lent, ezért a notify-hívás egy injektált callback mögött van, hogy a sorrend ne kösse meg); `OFFICIAL_GROUP_SLUG` / official group lookup.
- Produces:
  - `async function evaluateMatchBadges(matchId: string): Promise<void>` — a meccsre a hivatalos Ranglista-csoportban tippelő userök; mindegyikre: betölti a user **összes** lepontozott hivatalos-Ranglista tippjét időrendben → `computeAbsoluteMatchBadges` → `diffBadge` a `loadUserBadges`-hez → `upsertUserBadge` + gyűjti a `new`/`upgraded` badge-eket → `createBadgeNotifications(userId, increments)`.
  - `async function evaluateRoundBadges(tournamentId: string, round: string): Promise<void>` — `perfect_day` (abszolút, derive a userök tippjeiből), `round_winner` (nettó a `token_ledger`-ből → `roundWinners` → `recordBadgeEvent` → count → upsert), `throne` (`currentLeader` a hivatalos ranglistán → `recordBadgeEvent(tournamentId)`).
  - `async function evaluateTournamentBadges(tournamentId: string): Promise<void>` — `champion`/`podium`/`red_lantern` (`tournamentPlacements`), `oracle` (pontos dobogós pozíciók a `podium_bets`-ből) — mind `recordBadgeEvent` → count → upsert.
  - `async function isRoundComplete(tournamentId: string, round: string): Promise<boolean>` — nincs `scheduled`/`live` meccs az adott `round`-ban.

**Részletek:**
- Az official group lookup: `db.query.groups.findFirst({ where: and(eq(groups.tournamentId, id), eq(groups.isOfficial, true)) })`.
- A hivatalos-Ranglista tippek betöltése egy userre: join `bets` ⋈ `matches` `WHERE bets.groupId = officialGroupId AND bets.userId = ? AND bets.payout IS NOT NULL ORDER BY matches.scheduledAt, bets.matchId`. Map → `ScoredBet` (`oddsAtBet` Number-re, `result1x2Correct`/`exactScoreCorrect` `?? false`).
- A forduló-nettó: `token_ledger` `amount` összege a hivatalos csoportra, a `referenceId → bets → matches.round = round` szűréssel, `type IN ('bet','win','refund')`, userId-nként. (A `bet` típus negatív tét, a `win`/`refund` pozitív → a nettó a forduló eredménye.)
- Event-badge count: `recordBadgeEvent` → ha `true`, `countBadgeEvents` → `upsertUserBadge({ badgeKey, tier: tierFor(def, count), value: count, count, bestValue: null })`; az increment listába a `tierFor` előtti/utáni összevetésből (vagy egyszerűbb: ha `recordBadgeEvent` true ÉS az új tier > régi tier → notify).

- [ ] **Step 1: Implementáció** `src/lib/badges/award.ts` a fenti interfész szerint. Minden hivatalos-csoport hiány / üres mezőny esetén korai `return` (no-op). Hibatűrés: az award soha ne dobjon a scoring-flow-ba — a teljes törzset `try/catch`-eld, és `console.error`-rel logolj (a pontozás nem bukhat el badge-hiba miatt).

- [ ] **Step 2: Build** — `npm run check`.

- [ ] **Step 3: Commit**

```bash
git add src/lib/badges/award.ts
git commit -m "feat(badges): award orchestráció (meccs/forduló/torna evaluáció)"
```

---

## Task 8: Aggregált értesítés + display-fix + i18n

**Files:**
- Modify: `src/lib/notifications.ts`, `src/lib/notification-display.ts`
- Modify: `messages/hu.json`, `messages/en.json`
- Test: `tests/lib/notification-display.test.ts` (ha létezik, bővítés; különben új a badge-ágra)

**Interfaces:**
- Consumes: `createNotificationForUser` (meglévő); `getBadge` (Task 2).
- Produces:
  - `interface BadgeIncrement { badgeKey: string; tier: number; count: number; bestValue: number | null; change: "new" | "upgraded" }`
  - `async function createBadgeNotifications(userId: string, increments: BadgeIncrement[]): Promise<void>` — üres → no-op; 1 elem → `createNotificationForUser({ type:"badge", data:{ badgeKey, tier, count, bestValue }, href:"/u/"+userId })`; több → `createNotificationForUser({ type:"badge", data:{ aggregate:true, count: increments.length }, href:"/u/"+userId })`.

- [ ] **Step 1: `createBadgeNotifications`** hozzáadása `src/lib/notifications.ts`-hez (a `href` a saját profilra mutat, lokále-prefix nélkül — a notification-lista a kattintáskor a meglévő locale-aware navigációt használja).

- [ ] **Step 2: `notification-display.ts` badge-ág** (a spec szerinti, `data`-átadással + aggregált ág):

```ts
if (n.type === "badge") {
  const data = (n.data ?? {}) as { badgeKey?: string; aggregate?: boolean; tier?: number; count?: number; [k: string]: unknown };
  if (data.aggregate) {
    return { title: t("badge.aggregate.title"), body: t("badge.aggregate.body", { count: Number(data.count ?? 0) }) };
  }
  const badgeKey = data.badgeKey ?? "unknown";
  return {
    title: t(`badge.${badgeKey}.title`),
    body: t(`badge.${badgeKey}.body`, { count: Number(data.count ?? 0), tier: Number(data.tier ?? 1) }),
  };
}
```

- [ ] **Step 3: i18n** — `messages/hu.json` és `en.json` `notifications` namespace alá `badge` objektum: `aggregate.title`/`aggregate.body` + minden badge-kulcsra `<key>.title`/`<key>.body` (ICU `{count}`-tal). Magyar példák ékezetekkel:

```jsonc
// hu.json -> notifications.badge
"badge": {
  "aggregate": { "title": "Új jelvények!", "body": "{count} új jelvényt szereztél." },
  "win_streak": { "title": "Nyerő széria", "body": "{count} nyertes tipp egymás után!" },
  "exact_streak": { "title": "Telitalálat", "body": "{count} pontos eredmény egymás után!" },
  "jackpot": { "title": "Jackpot", "body": "Behoztál egy nagy oddsú tippet!" },
  "perfect_day": { "title": "Nyerő nap", "body": "Egy teljes napod nyertes lett!" },
  "oracle": { "title": "Jósda", "body": "Eltaláltál egy dobogós pozíciót!" },
  "round_winner": { "title": "Fordulógyőztes", "body": "Tiéd a forduló legjobb eredménye!" },
  "champion": { "title": "Bajnok", "body": "Megnyertél egy tornát!" },
  "podium": { "title": "Dobogós", "body": "Dobogóra értél egy tornán!" },
  "throne": { "title": "Trónfoglaló", "body": "Vezetted a Ranglistát!" },
  "red_lantern": { "title": "Piros lámpás", "body": "Utolsó lettél — legközelebb jobb lesz!" }
}
```

(en.json: ugyanezek angolul.)

- [ ] **Step 4: Test** a display badge-ágra (egyszeri + aggregált), majd `npm run test -- notification-display`.

- [ ] **Step 5: Commit**

```bash
git add src/lib/notifications.ts src/lib/notification-display.ts messages/ tests/
git commit -m "feat(badges): aggregált badge-értesítés + display data-átadás + i18n"
```

---

## Task 9: Trigger-bekötés a scoring-flow-ba

**Files:**
- Modify: `src/lib/sync.ts` (`scoreMatch` ~494 után hívás; `syncFixtures` forduló-check)
- Modify: `src/actions/admin.ts` (`finishTournament` ~260 után)

**Interfaces:**
- Consumes: `evaluateMatchBadges`, `evaluateRoundBadges`, `evaluateTournamentBadges`, `isRoundComplete` (Task 7).

- [ ] **Step 1: Meccs-trigger.** A `scoreMatch` végén (a `for` ciklus után, sor ~494), `await evaluateMatchBadges(matchId)` — try/catch a függvényen belül van (Task 7), de a hívás helye a status-flip nyertes ágában legyen (csak ha tényleg pontoztunk). A `syncFixtures`-ben, ahol a `scheduled→finished` flip sikeres és a `scoreMatch` lefutott, a meccs `round`-jára: ha `await isRoundComplete(tournamentId, round)` → `await evaluateRoundBadges(tournamentId, round)`.

- [ ] **Step 2: Torna-trigger.** `finishTournament` végén (sor ~260, a `for` után): `await evaluateTournamentBadges(tournamentId)`.

- [ ] **Step 3: Build + a meglévő sync/scoring tesztek** — `npm run test` → minden zöld (a badge-hívások no-op-olnak hivatalos csoport / adat híján a unit-szinten).

- [ ] **Step 4: Commit**

```bash
git add src/lib/sync.ts src/actions/admin.ts
git commit -m "feat(badges): trigger-bekötés (meccs/forduló/torna pontozás)"
```

---

## Task 10: Badge ikon komponens + tier-színek

**Files:**
- Create: `src/components/badge-icon.tsx`
- Modify: `src/app/globals.css` (`@theme`: `--color-bronze`, `--color-silver`)

**Interfaces:**
- Consumes: `getBadge` (Task 2).
- Produces: `BadgeIcon({ badgeKey, tier, size }: { badgeKey: string; tier: number; size?: number })` — a katalógus `icon` mezőjéből Lucide ikont rendel (statikus import-map, nem dinamikus), a tier szerint `text-bronze`/`text-silver`/`text-gold` (tier 0 → `text-faint`).

- [ ] **Step 1:** `globals.css @theme`-be: `--color-bronze: #cd7f32;` `--color-silver: #c0c0c0;` (a `text-gold` már létezik).
- [ ] **Step 2:** `badge-icon.tsx` — explicit Lucide import-map (`dice-5`→`Dice5`, `target`→`Target`, `gem`→`Gem`, `sun`→`Sun`, `sparkles`→`Sparkles`, `medal`→`Medal`, `crown`→`Crown`, `award`→`Award`, `star`→`Star`, `flame`→`Flame`), `aria-label` az i18n badge-névből (a komponens `useTranslations("badges")`-t használ).
- [ ] **Step 3:** `npm run check`.
- [ ] **Step 4: Commit**

```bash
git add src/components/badge-icon.tsx src/app/globals.css
git commit -m "feat(badges): BadgeIcon komponens + bronz/ezüst tier-tokenek"
```

---

## Task 11: Profil query + statisztikák (TDD a pure statra)

**Files:**
- Create: `src/queries/profile.ts`
- Create: `src/lib/profile-stats.ts` (pure számítások)
- Test: `tests/lib/profile-stats.test.ts`

**Interfaces:**
- Produces:
  - `src/lib/profile-stats.ts`: `interface BetStatInput { result1x2Correct: boolean | null }` → `function hitRate(bets: BetStatInput[]): number` (0–100, lepontozottak közül a `result1x2Correct` arány; üres → 0).
  - `src/queries/profile.ts`: `async function getProfile(userId: string, viewerId: string): Promise<ProfileView | null>` — `null` ha nincs **közös hivatalos Ranglista vagy kör** (láthatóság); egyébként `{ displayName, avatarUrl, badges: StoredBadge&{badgeKey}[], stats: { totalBets, hitRate, bestWinStreak, biggestJackpotOdds, placements } }`.
  - `async function shareACircleOrLeague(userId: string, viewerId: string): Promise<boolean>` — közös `group_members` (isOfficial csoport) VAGY közös `circle_members`.

- [ ] **Step 1: Failing test** `tests/lib/profile-stats.test.ts` a `hitRate`-re (üres → 0; 1/2 helyes → 50; null-ok kihagyva).
- [ ] **Step 2: Run → FAIL.**
- [ ] **Step 3:** `profile-stats.ts` implementáció (a `bestWinStreak`/`biggestJackpotOdds` a `userBadges` `count`/`bestValue` mezőből jön a query-ben, nem itt).
- [ ] **Step 4: Run → PASS.**
- [ ] **Step 5:** `src/queries/profile.ts` — láthatóság-ellenőrzés + adat-összeállítás (badges a `loadBadgesForUsers([userId])`-ből; statok: `totalBets`/`hitRate` a hivatalos-Ranglista `bets`-ből, `bestWinStreak`/`biggestJackpotOdds` a `userBadges`-ből).
- [ ] **Step 6: Commit**

```bash
git add src/queries/profile.ts src/lib/profile-stats.ts tests/lib/profile-stats.test.ts
git commit -m "feat(badges): profil query + statisztika (hitRate tesztelt)"
```

---

## Task 12: Profil-oldal + trófea-szekrény

**Files:**
- Create: `src/app/[locale]/u/[userId]/page.tsx`
- Create: `src/components/trophy-cabinet.tsx`
- Modify: a meglévő user-menü/`nav.tsx` — „Profilom" link a saját `/u/[id]`-re.

**Interfaces:**
- Consumes: `getProfile` (Task 11), `BadgeIcon` (Task 10), `getCurrentUser`, `BADGES` (Task 2).

- [ ] **Step 1: `trophy-cabinet.tsx`** — a `BADGES` katalógus minden eleme rácsban (`grid grid-cols-3 sm:grid-cols-5 gap-3`); a megszerzett badge `BadgeIcon` a tier-színben + `×count`/szint felirat; a meg-nem-szerzett `tier 0` (halvány). Popover/tooltip a leírással (`badges.<key>.title`/`.description`). Minden szöveg `useTranslations("badges")`.
- [ ] **Step 2: `page.tsx`** — server component: `getCurrentUser()` → `getProfile(params.userId, viewer.id)`; ha `null` → `notFound()`; fejléc (avatar + becenév), `TrophyCabinet`, stat-kártyák (összes tipp, találati arány, legjobb sorozat, legnagyobb jackpot odds, torna-helyezések). A meglévő `bg-surface`/`text-foreground`/`text-gold` tokenekkel.
- [ ] **Step 3: i18n** — `badges` namespace `hu.json`/`en.json`: minden badge `title`/`description` + a profil-oldal feliratai (`profile.*` bővítés: `trophies`, `stats`, `totalBets`, `hitRate`, `bestStreak`, `biggestJackpot`, `placements`, `notVisible`).
- [ ] **Step 4: Self-test** — `npm run dev`, megnyitni `/hu/u/<saját id>` → szekrény + statok renderelnek; idegen (közös kör nélküli) user → 404.
- [ ] **Step 5: Commit**

```bash
git add src/app/\[locale\]/u src/components/trophy-cabinet.tsx src/components/nav.tsx messages/
git commit -m "feat(badges): publikus profil-oldal + trófea-szekrény"
```

---

## Task 13: Ranglista ikon-sor

**Files:**
- Create: `src/components/leaderboard-badges.tsx`
- Modify: a `getGroupLeaderboard` fogyasztó(i) (`GroupLeaderboardContent` és a kör-nézet) + a betöltő server component, hogy átadja a badge-eket.

**Interfaces:**
- Consumes: `loadBadgesForUsers` (Task 6), `BadgeIcon` (Task 10).
- Produces: `LeaderboardBadges({ badges }: { badges: Array<{ badgeKey: string; tier: number }> })` — a top **3 legmagasabb tier**ű badge (tier desc, majd katalógus-sorrend), `BadgeIcon size={16}` sorban; üres → semmi.

- [ ] **Step 1:** A ranglistát betöltő server component a sorok userId-jaira `loadBadgesForUsers(userIds)` (egy batch), és a map-et leadja a kliens-komponensnek.
- [ ] **Step 2:** `leaderboard-badges.tsx` + beillesztés a név mellé (a sor a `/u/[userId]`-re linkel).
- [ ] **Step 3:** `npm run check` + vizuális ellenőrzés `npm run dev`.
- [ ] **Step 4: Commit**

```bash
git add src/components/leaderboard-badges.tsx src/components/
git commit -m "feat(badges): ranglista ikon-sor a név mellett"
```

---

## Task 14: Teljes verifikáció + záró commit

- [ ] **Step 1:** `npm run test` → minden zöld.
- [ ] **Step 2:** `npm run check` → nincs lint/type hiba.
- [ ] **Step 3:** `npm run build` → sikeres production build.
- [ ] **Step 4:** Kézi smoke a dev Ranglistán: tipp lepontozása (vagy admin sync) → harang-értesítés megjelenik → `/u/[id]` szekrény frissül → ranglista-ikon látszik.
- [ ] **Step 5:** `ARCHITECTURE.md` — rövid „Badge-rendszer" szekció (katalógus, derive-and-diff, trigger-pontok, hatókör). Commit:

```bash
git add ARCHITECTURE.md
git commit -m "docs(badges): badge-rendszer szekció az architektúrában"
```

---

## Self-Review (terv ↔ spec)

**Spec coverage:**
- Katalógus + küszöbök → Task 2 ✓ · Hatókör (hivatalos Ranglista) → Task 7 ✓ · Streak/jackpot/perfect-day → Task 3-4 ✓ · Relatív badge-ek → Task 5,7 ✓ · Adatmodell (kód-katalógus + `user_badges` + event-tábla) → Task 1,2 ✓ · Derive-and-diff → Task 3-7 ✓ · Trigger-pontok → Task 9 ✓ · Aggregált értesítés + display-fix → Task 8 ✓ · Profil-oldal + statok → Task 11-12 ✓ · Ranglista ikon-sor → Task 13 ✓ · Grafika (Lucide+tier) → Task 10 ✓ · i18n → Task 8,12 ✓ · Tesztelés → Task 2-5,11 ✓ · Migráció → Task 1 ✓.
- **Spec-eltérés (szándékolt):** a spec „kód-katalógus, nincs DB-katalógus" — a terv ezt követi; a `user_badge_events` tábla a specben implicit volt (idempotencia), a terv explicitté teszi a relatív badge-ekhez. Indokolt: a „Trónfoglaló/valaha vezetett" nem derive-álható végállapotból.

**Placeholder scan:** nincs „TBD/TODO"; a pure logika teljes kóddal; a DB/UI taskok konkrét interfésszel és kulcs-kóddal (a UI a meglévő design-token mintából épül).

**Type consistency:** `ScoredBet`/`BadgeProgress`/`StoredBadge`/`BadgeIncrement`/`RankRow` végig egységes; `tierFor` `0|1|2|3`; `BADGES` kulcsok a katalógus és az i18n közt egyeznek.
